// This program *calculates* **the volume of a cylinder**

let diameter = 10;
let height = 20;

let radius = diameter / 2;
let volume = Math.PI * radius * radius * height;

console.log(`A cylinder with diameter ${diameter} meters and height ${height} meters has a volume of ${volume} cubic meters`);
